[alias]
  co = checkout
  ci = commit
  br = branch
  st = status
  hist = log --all --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit 
  # %h short hash, %s commit subject, %ad author date, %an author name, %d ref name
  

[merge]
    tool = kdiff3

[mergetool "meld"]
    path = "C:/Program Files (x86)/Meld/Meld.exe"
    trustExitCode = false

[diff]
    tool = meld

[difftool "meld"]
    path = "C:/Program Files (x86)/Meld/Meld.exe"
	  prompt = false
    trustExitCode = false

[color]
	ui = auto

[user]
  name = mat
  email = mat@html.com

[push]
  default = simple

[core]
  editor = vim
